# legacy-cleanup Specification

## Purpose

This capability is **retired**. `qaspec init` and `qaspec update` no longer detect or clean up legacy OpenSpec-era artifacts. Any stale files in a project are inert and can be deleted manually.

## Requirements

_No active requirements. All prior legacy-cleanup requirements were removed in the purge-openspec-legacy change._

# openspec-coexistence Specification

## Purpose

This capability is **retired**. QASpec no longer detects or coexists with an upstream OpenSpec installation. QASpec manages only the artifacts it generates under `qaspec/`.

## Requirements

_No active requirements. All prior coexistence requirements were removed in the purge-openspec-legacy change._
